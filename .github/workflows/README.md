# Workflows

This directory contains GitHub Actions quality gates. The M1 workflow installs the project on Python 3.12 and runs the same formatting, linting, type, coverage, and test checks used locally.
