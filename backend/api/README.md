# API

Owns external transport concerns such as HTTP routes, request and response translation, validation at the system boundary, and API versioning. It delegates use cases to services and contains no scheduling or persistence policy.
