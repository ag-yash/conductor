# Core

Owns application-wide bootstrap and cross-cutting primitives that cannot belong to a narrower module, such as lifecycle coordination and shared error taxonomy. Domain and service logic should not accumulate here.
