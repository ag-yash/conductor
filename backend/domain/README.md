# Domain

Owns technology-independent entities, value objects, state transitions, and invariants for jobs, workers, models, and executions. It must remain independent of web frameworks, databases, and model runtimes.
