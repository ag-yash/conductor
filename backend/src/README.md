# Python source

This directory is the import root for Conductor's installable Python package. Keeping source below an explicit import root prevents tests from accidentally importing files directly from the repository checkout.
