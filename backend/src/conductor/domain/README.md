# Domain

Owns framework-independent entities, value objects, invariants, and legal state transitions. M2 introduces jobs and execution attempts here without importing FastAPI, SQLModel, or database code.
