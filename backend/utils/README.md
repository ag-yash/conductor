# Utilities

Contains small, stateless helpers with clear reuse across modules. New code should live in the module that owns it unless it is genuinely generic; this directory must not become a miscellaneous dependency bucket.
