# Dashboard

This directory will contain the React and TypeScript operational interface for inspecting jobs, workers, models, queues, and resource usage. Frontend tooling will be initialized with the dashboard milestone to avoid speculative dependencies.
