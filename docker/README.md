# Docker

Contains container build files, entrypoints, and container-specific configuration. Root-level Compose configuration defines local topology; images will be added only when executable components exist.
