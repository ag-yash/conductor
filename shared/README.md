# Shared contracts

Contains deliberately shared, versioned contracts or test fixtures used by more than one deployable component. It should stay small; backend-internal reuse belongs under the owning backend module.
