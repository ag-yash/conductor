# Tests

Contains repository-level, integration, contract, and end-to-end tests. Unit tests may live near their owning Python modules once code exists; all tests should be deterministic and classify external-resource requirements explicitly.
